<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice %>
    <p class="alert alert-success"><<%= notice %></p>
    <% end %>
    <% if alert %>
    <p class="alert alert-danger"><<%= alert %></p>
    <% end %>
    <div class="flex flex-row">
      <p class="float-left">
      <%= link_to "Home", root_path, class: "navbar_link" %>
      </p>
    <p class="navbar-text float-right">
      <% if user_signed_in? %>
      Logged in as <strong><%= current_user.username %></strong>
      <%= link_to "Edit Profile", edit_user_registration_path, class: "navbar_link" %> |
      <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "navbar_link" %>
      <% else %>
      <%= link_to "Sign Up", new_user_registration_path, class: "navbar_link" %>|
      <%= link_to "Login", new_user_session_path, class: "navbar_link" %>
      <% end %>
    </p>
    </div>
    <%= yield %>
  </body>
</html>
